#!/usr/bin/env bash
# Profile: brave-spotify
# Generated by Semsumo v6.3.0
set -e

echo "Initializing brave-spotify..."

# Switch to workspace
if [[ "8" != "0" ]] && command -v hyprctl >/dev/null 2>&1; then
    CURRENT_WORKSPACE=$(hyprctl activeworkspace -j | grep -o '"id": [0-9]*' | grep -o '[0-9]*' || echo "")
    
    if [[ "$CURRENT_WORKSPACE" != "8" ]]; then
        echo "Switching to workspace 8..."
        hyprctl dispatch workspace "8"
        sleep 1
        echo "Waiting 1 seconds for transition..."
    else
        echo "Already on workspace 8, skipping switch."
    fi
fi

echo "Starting application..."
echo "COMMAND: profile_brave --spotify --class spotify --title spotify"
echo "VPN MODE: secure"

# Start application with VPN mode
case "secure" in
    bypass)
        if command -v mullvad >/dev/null 2>&1 && mullvad status 2>/dev/null | grep -q "Connected"; then
            if command -v mullvad-exclude >/dev/null 2>&1; then
                echo "Starting with VPN bypass (mullvad-exclude)"
                mullvad-exclude profile_brave --spotify --class spotify --title spotify &
            else
                echo "WARNING: mullvad-exclude not found, starting normally"
                profile_brave --spotify --class spotify --title spotify &
            fi
        else
            echo "VPN not connected, starting normally"
            profile_brave --spotify --class spotify --title spotify &
        fi
        ;;
    secure|*)
        if command -v mullvad >/dev/null 2>&1 && mullvad status 2>/dev/null | grep -q "Connected"; then
            echo "Starting with VPN protection"
        else
            echo "WARNING: VPN not connected! Starting without protection"
        fi
        profile_brave --spotify --class spotify --title spotify &
        ;;
esac

# Save PID
APP_PID=$!
mkdir -p "/tmp/sem"
echo "$APP_PID" > "/tmp/sem/brave-spotify.pid"
echo "Application started (PID: $APP_PID)"

# Make fullscreen if needed
if [[ "true" == "true" ]]; then
    echo "Waiting 1 seconds for application to load..."
    sleep 1
    
    if command -v hyprctl >/dev/null 2>&1; then
        echo "Making fullscreen..."
        hyprctl dispatch fullscreen 1
    fi
fi

# Switch to final workspace if different
if [[ "8" != "0" ]]; then
    CURRENT_WORKSPACE=$(hyprctl activeworkspace -j | grep -o '"id": [0-9]*' | grep -o '[0-9]*' || echo "")
    
    if [[ "$CURRENT_WORKSPACE" != "8" ]]; then
        echo "Switching to final workspace 8..."
        if command -v hyprctl >/dev/null 2>&1; then
            hyprctl dispatch workspace "8"
        fi
    else
        echo "Already on final workspace 8, skipping switch."
    fi
fi

exit 0
